T = int(input())
for case in range(T):
    N, K = map(int, input().split(' '))
    C = list(map(int, input().split(' ')))
    D = list(map(int, input().split(' ')))
    # for given R, what's the max required index leftward?
    Rs = [0] * N
    for R in range(N):
      if it works at R
      for L in range(R-1, -1, -1):
        
    # count = 0
    # for L in range(N):
        # for R in range(L, N):
            # if abs(max(C[L:R+1]) - max(D[L:R+1])) <= K:
                # count += 1
    # print("Case #" + str(case+1) + ": " + str(count))

